{% extends 'admin/layout.html.twig' %}

{% block title %}Paramètres CMS - Bazma Kebili{% endblock %}
{% block html_lang %}fr{% endblock %}
{% block html_dir %}ltr{% endblock %}

{% block meta_robots %}noindex,nofollow{% endblock %}

{% block workspace %}

<main class="admin-layout admin-workspace">
    {% set successMessages = app.flashes('success') %}
    {% set errorMessages = app.flashes('error') %}
    {% if successMessages or errorMessages %}
        <div class="admin-flashes" role="status" aria-live="polite">
            {% for message in successMessages %}<p class="success admin-flash">{{ message }}</p>{% endfor %}
            {% for message in errorMessages %}<p class="alert admin-flash">{{ message }}</p>{% endfor %}
        </div>
    {% endif %}

    <section class="admin-hero-panel">
        <div>
            <p class="admin-eyebrow">Référencement et global</p>
            <h1>Paramètres du site</h1>
        </div>
        <div class="admin-quick-actions">
            {% if is_granted('ROLE_HOME_MANAGER') %}<a class="button" href="{{ path('admin_home_settings') }}">Modifier l’accueil</a>{% endif %}
            <a class="button secondary-admin" href="{{ path('admin_setting_new') }}">Nouveau paramètre</a>
        </div>
    </section>

    <section class="admin-content-section" data-cms-list>
        <div class="admin-section-head">
            <div>
                <p class="admin-eyebrow">Clés utiles</p>
                <h2>Configuration avancée</h2>
            </div>
        </div>
        <div class="settings-hints">
            <span>seo_title_ar/fr/en</span>
            <span>seo_description_ar/fr/en</span>
            <span>og_image</span>
            <span>footer_text_ar/fr/en</span>
        </div>
        {% include 'admin/list_tools.html.twig' %}
        <div class="admin-list">
            {% for setting in settings %}
                {% set key = setting.settingKey %}
                {% set language = key|split('_')|last %}
                {% set baseKey = language in ['fr', 'ar', 'en'] ? key|slice(0, -3) : key %}
                {% set labels = {seo_title: 'Titre dans les moteurs de recherche', seo_description: 'Description dans les moteurs de recherche', og_image: 'Image de partage', footer_text: 'Texte du pied de page', site_title: 'Titre du site', site_intro: 'Présentation du village', brand: 'Nom du site', approval_workflow_enabled: 'Validation avant publication'} %}
                {% set category = key starts with 'seo_' ? 'Référencement' : (key starts with 'footer_' ? 'Pied de page' : (key starts with 'og_' ? 'Partage' : 'Général')) %}
                <article class="admin-item">
                    <div class="admin-item-main">
                        <span class="status-pill">{{ category }}</span>
                        <h3>{{ attribute(labels, baseKey)|default(baseKey|replace({'_': ' '})|capitalize) }}{% if language in ['fr', 'ar', 'en'] %} · {{ {fr: 'Français', ar: 'Arabe', en: 'Anglais'}[language] }}{% endif %}</h3>
                        <p>{{ setting.settingValue|slice(0, 140) }}{% if setting.settingValue|length > 140 %}...{% endif %}</p>
                    </div>
                    <div class="admin-item-actions">
                        <a href="{{ path('admin_setting_edit', {id: setting.id}) }}">Modifier</a>
                    </div>
                </article>
            {% else %}
                <p class="admin-empty">Aucun paramètre pour le moment.</p>
            {% endfor %}
        </div>
    </section>
</main>
{% endblock %}
